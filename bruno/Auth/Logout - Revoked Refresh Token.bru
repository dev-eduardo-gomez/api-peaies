meta {
  name: Logout - Revoked Refresh Token
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/auth/logout
  body: json
  auth: inherit
}

body:json {
  {
    "refreshToken": "{{refreshToken}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
