meta {
  name: Delete Vehicle
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/vehicles/{{vehicleId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
