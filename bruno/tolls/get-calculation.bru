meta {
  name: Get Calculation
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/tolls/history/{{calculationId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

vars:pre-request {
  calculationId: {{calculationId}}
}

settings {
  encodeUrl: true
  timeout: 0
}
